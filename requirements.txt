requests>=2.31.0
smbus2>=0.4.3
schedule>=1.2.0
PyYAML>=6.0.1
python-dotenv>=1.0.0
Flask>=3.0.0

# Optional — only needed if using a GPIO-based relay HAT instead of I2C
# RPi.GPIO>=0.7.1

# Dev / test
pytest>=8.0
pytest-mock>=3.14
